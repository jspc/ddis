#!/usr/bin/env ruby
#

$LOAD_PATH.unshift File.join(File.dirname(__FILE__), '..', 'lib')

require 'ddis'

ddis = DDis.new '10.0.2.0/24', 'unassigned'

ip = ddis.iterator.ip

until ip == nil
  ddis.test_ip ip
  ip = ddis.iterator.iterate
end
